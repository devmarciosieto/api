package endpoints

import (
	"net/http"
)

func (h *Handler) CampaignGet(w http.ResponseWriter, r *http.Request) (interface{}, int, error) {

	//campaign, err := h.CampaignService
	//return campaign, http.StatusOK, err

	return nil, http.StatusOK, nil
}
